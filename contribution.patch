diff --git a/main.py b/main.py
index 91885d5..f5df36d 100644
--- a/main.py
+++ b/main.py
@@ -56,6 +56,30 @@ def home():
 def get_movies():
     return movies

+#  SEARCH + PAGINATION
+@app.get("/movies/search")
+def search_movies(
+    keyword: Optional[str] = None,
+    min_rating: Optional[float] = None,
+    page: int = 1,
+    limit: int = 5
+):
+    results = movies
+
+    if keyword:
+        results = [m for m in results if keyword.lower() in m["name"].lower()]
+
+    if min_rating is not None:
+        results = [m for m in results if m["rating"] >= min_rating]
+
+    start = (page - 1) * limit
+    end = start + limit
+
+    return {
+        "total": len(results),
+        "data": results[start:end]
+    }
+
 @app.get("/movies/{movie_id}")
 def get_movie(movie_id: int):
     movie = find_movie(movie_id)
@@ -151,26 +175,3 @@ def cancel_booking(booking_id: int):
     bookings.remove(booking)
     return {"message": "Booking cancelled"}

-#  SEARCH + PAGINATION
-@app.get("/movies/search")
-def search_movies(
-    keyword: Optional[str] = None,
-    min_rating: Optional[float] = None,
-    page: int = 1,
-    limit: int = 5
-):
-    results = movies
-
-    if keyword:
-        results = [m for m in results if keyword.lower() in m["name"].lower()]
-
-    if min_rating is not None:
-        results = [m for m in results if m["rating"] >= min_rating]
-
-    start = (page - 1) * limit
-    end = start + limit
-
-    return {
-        "total": len(results),
-        "data": results[start:end]
-    }
\ No newline at end of file
diff --git a/requireents.txt b/requireents.txt
deleted file mode 100644
index e69de29..0000000
diff --git a/requirements.txt b/requirements.txt
new file mode 100644
index 0000000..c9b6004
--- /dev/null
+++ b/requirements.txt
@@ -0,0 +1,3 @@
+fastapi
+uvicorn
+pydantic
